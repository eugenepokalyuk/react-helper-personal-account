export type Experience = {
    period: string;
    duration: string;
    grade: string;
    job: string;
    points: string[];
    logo: string;
    videoThumbnail?: string;
    videoUrl?: string;
    imageUrl?: string;
}

export type MenuItem = {
    name: string;
};